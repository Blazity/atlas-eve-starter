{
  "name": "@repo/example-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "eve dev",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "eve:build": "eve build",
    "eval": "eve eval --skip-report"
  },
  "dependencies": {
    "@repo/example": "workspace:*",
    "ai": "7.0.0-beta.178",
    "eve": "0.11.8",
    "zod": "4.6.2"
  },
  "devDependencies": {
    "vitest": "4.1.9"
  }
}
